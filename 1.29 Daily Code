Daily Cdoe for 1.29.18

Intro: Ask the user for the radius of a circle. Then, print out its area and circumference.
Advanced: Ask a user for their age. Then print that age out in a huge font in a game window, with the words, "Happy XXth Birthday!"
(where the XX is the number they gave you). 


#include<allegro5\allegro.h>
#include <allegro5/allegro_font.h>
#include <allegro5/allegro_ttf.h>
#include <iostream>
using namespace std;

int age = 40;

int main() {

	ALLEGRO_DISPLAY *gamewindow = NULL;

	al_init();

	al_init_font_addon();
	al_init_ttf_addon();

	cout << "whats your age" << endl;
	cin >> age;

	ALLEGRO_FONT *font = al_load_ttf_font("28 Days Later.ttf", 72, 0);

	gamewindow = al_create_display(640, 480);

	al_set_window_position(gamewindow, 200, 200);

	al_set_window_title(gamewindow, "Its your Boi uhhhhh...");

	//render section///////////////////////////////////////////
	al_clear_to_color(al_map_rgb(50, 10, 70));

	al_draw_textf(font, al_map_rgb(255, 255, 255), 200, 300, ALLEGRO_ALIGN_CENTRE, "Happy %d", age);

	al_flip_display();

	//////////////////////////////////////////////////////////////

	al_rest(10.0);

	al_destroy_display(gamewindow);

}
